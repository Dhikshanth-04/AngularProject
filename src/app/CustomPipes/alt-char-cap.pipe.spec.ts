import { AltCharCapPipe } from '../alt-char-cap.pipe';

describe('AltCharCapPipe', () => {
  it('create an instance', () => {
    const pipe = new AltCharCapPipe();
    expect(pipe).toBeTruthy();
  });
});
